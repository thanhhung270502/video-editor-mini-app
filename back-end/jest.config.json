{
  "preset": "ts-jest",
  "testEnvironment": "node",
  "roots": ["<rootDir>/src"],
  "testMatch": ["**/*.test.ts"],
  "setupFiles": ["<rootDir>/src/test-setup.ts"],
  "transform": {
    "^.+\\.tsx?$": "ts-jest"
  }
}
